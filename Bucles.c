#include <stdio.h>
 
int main()
{
    int num = 0; // Condicion inicial
    while(num >= 10) // Condicion final
    {
        printf("repiticion: %d .- Hola este mensaje se repite\n", num);
        num++; // Incremento 
    }
    return 0;
}